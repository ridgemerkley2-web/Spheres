# S08 runtime requalification notes

Outside-repository working note, updated 13 September 2026 UTC (12 September Pacific). The replacement runtime is committed as `d770592aeead51f1313d507edd26b02d75a69bba`; full qualification is running. This note records implemented corrections, their intended contracts and completed diagnostic outcomes; it awards no completion or replacement qualification.

## Player outcome

S08 provides finite domestic and foreign supplier stock through Government → Companies → Compare equipment suppliers. Players inspect the exact model, review a current price and quantity, and follow an owned import through escrow and delivery. Cancellation remains a separate reviewed transaction, subject to the original seller's ability to receive the returned lot. Imported specifications grant ordinary use and maintenance rights without component research or a domestic manufacturing licence.

Seven modeled supplier programmes must fund facilities, development, tooling and actual inputs. They skip the player, require economic competition and remain opt-in on legacy loads. Dated work and blockers describe availability; no historical opening fleet or guaranteed first-stock date is supplied. The current S08 README records the explicit Tonga budget choice, including the Infrastructure-to-Defense transfer and annual renewal. That policy creates no immediate cash, authority, stock or research grant.

## Corrections in the current working tree

- Company commands generally retain one isolated native trial and commit it only after the unchanged political-cost check. Refused orders discard trial effects. A narrow capitalization shortcut can use the unchanged native command directly only when its actual political bill and policy journal entry are absent, all living enrolled budgets are already open, and every company/import settlement hook is idle. Due receipts of any kind, import escrow settlement or cancellation refunds, and unopened budgets retain the original whole-world trial and rollback. Priced establishment retains its standing check before commit. Equipment/company action eligibility uses existing native refusal checks; actual approvals still revalidate current state.
- Supplier programme reviews reuse the exact working-capital arithmetic for their already validated design. They generate the full development quote, including its current token and timing, only when an empty catalogue actually needs a development order. Capitalization still precedes development validation as before; existing products, insufficient authority and outstanding paid obligations retain their original decisions. Public development quotes and actual commands remain fully validated.
- Equipment and ammunition sections share supplier reads within one response. Completed nation/relation JSON arrays move into their parent objects instead of being copied again. History formats each distinct retained NationId once; it still includes the full retained archive and preserves delta selection and available-nation ordering.
- Freight clearing reuses bounded nominal searches and exact access checks, including a 256-tree retention limit. Within one clearing, immutable endpoint permission reads also serve bilateral eligibility checks; blocked cases retain native refusal precedence for identical endpoints, inactive countries, sanctions and war. Search ties and congestion budgets retain their existing rules. Live dispatch quantities, edge reservations, alternate-route selection and experience-sensitive contractor work remain authoritative. Cached route permissions are scoped to their clearing or immutable read and cannot outlive changes to diplomacy, control or the calendar.
- Contract forecasts still simulate every dated arrival, stock limit, expiry and dispatch. A single previous capacity result is reused only for identical ordered bundle IDs, legs and quantity bits on the same immutable world. Civilian demand, Materials horizon arrays, industry capacity and pending freight avoid repeated identical reads within their valid scopes.
- National industry and energy reads filter operating districts by current ownership before cloning names. They retain the same sorted, unique district order and each caller's existing contested, alive and enrollment rules. Power dispatch uses the owner already present in its district-map row. These reads change neither ownership nor operating capacity.
- Economic AI can select the first acceptable manufactured-goods quote without computing every later seller quote. Ordering, refusal precedence, prices, financial limits and standalone purchase validation remain unchanged. Monthly raw-material purchasing evaluates routes as sellers are reached; its civilian first-reachable check preserves the shared draw memo when no route exists. Commands, review cadence and RNG ordering are retained.
- The civilian recurring-purchase pass omits a buyer's full forecast only when every non-oil installed operating-demand component is exactly zero. In that case its native civilian gap cannot create an ask or update the shared draw memo, even if finite projects or military demand exist. Any other component value, including malformed non-finite values, keeps the original forecast path. The military pass and the shared dated contract projection remain in place.
- Optional stage diagnostics observe execution without storing timings in game state or reading clocks on ordinary paths. The supplier browser harness releases responses and compares archives through a short-lived worker, retaining full canonical comparisons and signed values instead of keeping several complete worlds in Node. This harness change still requires a complete new browser journey.

These changes reduce repeated calculation and allocation. Existing funding and input rules remain in force; no stock or company funding is supplied, no history is suppressed and scheduled opponents are not postponed to improve a benchmark.

## Latest completed corrective checks

The completed round 17 batch passed 69 checks. Its feature 6 performance run still failed the fixed percentile limits: simulation/history p95 was **384.8299 ms** against 300 ms, and whole server-turn p95 was **490.2073 ms** against 400 ms. Passing focused checks did not establish acceptable campaign performance.

The round 18 focused run recorded 33 passing tests and two fixture failures before the intended foreign-budget/import fallback checks: each synthetic UK enrollment was attempted while France held the player seat and correctly met the normal directing-government refusal. The fixture correction temporarily seats UK for its ordinary enrollment command, then restores France before quoting capitalization. Round 19 recorded 34 passing tests and a third fixture error: the intended idle future-import case still had an unopened UK budget, so the shortcut correctly fell back. The fixture now opens all daily budgets through the ordinary begin-day hook and explicitly asserts that the future import payment is not due. No runtime funding or permission guard was relaxed. Round 20 then passed all 74 focused checks, with four diagnostic tests ignored. The original failures remain evidence.

Round 20 feature 7 still failed the timing limits: simulation/history p95 was **374.3009 ms**, whole-turn p95 **466.1381 ms**, maximum **627.7783 ms**, equipment-board p95 **103.4075 ms** and quote p95 **1.3470 ms**. Daily diagnostic 8 matched the complete native world on all 31 days. Idle supplier capitalization fell below 4 ms at maximum, compared with the preceding diagnostic's approximately 45 ms supplier p95. These are exploratory measurements, not independent qualification.

Round 21 added a private civilian RUN forecast, preserving the public 30/90/365-day report. Its 30-day contract prefix, shared arithmetic and live buyer reads passed literal numeric-bit and two-buyer decision comparisons. Indexed static route assembly and opt-in heap counters preserve live terminal experience and the native queue order. All 76 focused checks passed, with four diagnostic tests ignored. Daily diagnostic 9 matched all 31 complete native worlds. Its busiest sampled days performed approximately 1.24–1.48 million heap pushes, with roughly 24 percent stale pops and a maximum active queue of 367 entries. Source traversal remains a material cost; no cache bound or gameplay search budget was raised.

Round 21 feature 8 still failed: simulation/history p95 **430.678 ms** versus 300 ms and whole-turn p95 **534.6809 ms** versus 400 ms. Its maximum **618.0245 ms**, equipment-board p95 **111.9325 ms** and quote p95 **1.5004 ms** met their individual limits. The run is retained despite slower percentile measurements. Machine timing variation and combined implementation changes between runs limit causal attribution; source and binary hashes remained fixed within each run. The outside `assess-s08-development-profile.py` records all five comparisons against the unchanged original plan for features 5–8. None uses a fresh replacement supplier input or qualifies a clean replacement source pin.

## Requalification and fixed limits

Round 22 added compact retained predecessor/rank storage and the private 30-day equipment/ammunition projection prefix. It also tested a four-way queue candidate. All focused and expanded regression suites passed: **142 test executions**, no failures, six ignored diagnostics; some checks occur in both the focused and full routing/supply filters. Daily diagnostic 10 matched all 31 native worlds. Feature 9 remained over the limits: simulation/history p95 **360.2399 ms**, whole-turn p95 **445.7688 ms**, maximum **623.7382 ms**, equipment-board p95 **114.4843 ms** and quote p95 **2.551 ms**. Compact storage reduced setup costs and its exact vector accounting saves 20 bytes per graph node/tree on x64. The four-way queue did not demonstrate a traversal benefit: source traversal measurements rose despite identical work counts. Its source and evidence remain in the feature 9 snapshot; the current implementation restores `BinaryHeap` and retains compact storage. No independent binary-heap-only attribution is claimed for that combined run.

The replacement candidate owns at most 256 nominal route trees in each live `Game`, outside `WorldState`, command trials and serialized archives. It validates complete mapped ownership and effective-control vectors plus routing mode, and still resolves complete per-tree permission keys on every clearing. Capacities, live terminal work, access/assembly reads, goals and the 32,768-node congestion budget remain fresh. Cold public tick/clearing entry points stay available as independent references. The observed subsystem diagnostic remains cold and the normal Game owns a separate initially empty pool; their search state is never shared.

The supplier browser harness now bounds its manually held preview waits, forwards request and cleanup errors, holds only the first matching request and removes only its own interceptor. Eight small VM route/error checks passed; this is not a replacement for the authentic browser journey. Outside patch, runner and hashed results are in `s08-staging/supplier-preview-timeout*` and must accompany the eventual evidence package.

Round 23 recorded **40 passes and one fixture assertion failure** before the remaining suites ran. The pooled clearing already matched the complete cold native world, but the disabled-market case then expected `stockpile()` to report stored inventory; that public function intentionally returns zero when the market is disabled. The correction reads the actual stored market ledger and retains the explicit disabled-view behavior. The original failed run remains evidence. The Game-owned pool is not yet qualified by this partial batch.

Round 24 passed **143 test executions**, no failures and six ignored diagnostics; focused and expanded routing/supply filters overlap. The pool's daily and legacy calendar hooks, month/leap/year transitions, commands, independent Games and save/load/reset behavior match the cold native reference. Daily diagnostic 11 matched every complete native world on all 31 days. Development feature 10 met all five original timing bars: simulation/history p95 **290.6901 ms**, whole-turn p95 **375.9804 ms**, whole-turn maximum **497.1219 ms**, equipment-board p95 **102.3519 ms**, and quote p95 **2.0868 ms**. Its profile SHA256 is `62e4fc9641833ec8c0086fbb7c80f96835726d5f404e4fdbae8ca28de0071fbe`. This dirty-source developmental run uses the original e618 genuine archive and does not qualify the replacement candidate. The only subsequent source change before pinning was to make the now-test-only `tick_day` import conditional on tests.

Clean runtime pin `d770592aeead51f1313d507edd26b02d75a69bba` is frozen during full Windows and Linux qualification. The new outside `bind-s08-performance.py` requires the clean pin, a successful full native proof and its exact test executable before binding the unchanged original plan for final measurement. Fresh supplier production and final browser/performance runs remain pending.

The initial `e6187df8044556a8262154e7a6c8baa32b283799` results belong to that exact source and its recorded executables. Its genuine-supplier performance acceptance failed, and its supplier browser journey ended before completion. Earlier native, browser or legacy-workload evidence cannot qualify changed simulation, command, read-model or harness code automatically. Retain those initial outcomes and every corrective diagnostic; bind replacement evidence to a clean new source pin, actual binaries, runners and input producers.

The declared limits remain unchanged: simulation/history p95 ≤300 ms; whole server-turn p95 ≤400 ms and maximum ≤750 ms; genuine supplier equipment-board read/serialization p95 ≤300 ms and purchase-quote p95 ≤250 ms. The six legacy workloads retain their 1 GiB sampled private-memory ceiling. Feature-workload memory remains an observation because that case had no declared memory acceptance bar. The original six legacy cases and 31-day genuine supplier workload must remain intact; focused parity tests and dirty-source diagnostics do not substitute for final acceptance.

Authentic supplier evidence must come from ordinary decisions and earned stock. Any reused initial archive must identify its original producer; replacement fresh stock, purchase, delivery and positive paid maintenance need their own dated evidence. The eleven-platform and 23-family domestic ammunition matrices, with representative imported `mg_127` and `air_bomb_guided` coverage, use disclosed synthetic prerequisites. They test mechanics and preservation, not fresh-world availability of every model or historical balance.

Use `README-requalification-draft.md` and the requalification checklist when assembling final evidence. The retained older final draft must not be applied as a completion record. S08 remains in progress; S09 and full campaign, content and release certification remain outside this closeout.
