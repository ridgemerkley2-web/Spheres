# Supplier final save-list failure: read-only source review

The corrected c74 harness run `evidence/S08-supplier-browser-corrected-route-pool-1/supplier-v6VJe7` passed stale-review rejection, actual purchase/delivery, midpoint exact Save/Load, and positive paid maintenance, then failed its original 30-second wait for `#saveSlots option[value="s08-import-complete"]`. The result and bound outer runner both remain failed; final Save/Load/Continue did not complete. Actual paid-service evidence is retained separately in `supplier-browser-corrected-route-pool-1-partial-receipt.json` with the original inner-result hash copied from the completed outer proof.

`spheres-web/src/storage.rs:196–225` lists every `saves/*.json`, reads each complete file into a string and parses it into `serde_json::Value` before reading only its date, player and format metadata. `main.rs:8132` runs this synchronously in the server's serial request loop. At review time, the failed disposable folder contained 21 JSON files totaling 2,560,752,214 bytes. Most are harness-generated purity/receipt snapshots, not user-created named campaigns.

`campaign-ui.js:149–157` disables the save button, awaits the real save request, then awaits `refreshSaveSlots()` before selecting the new slot and re-enabling controls. `refreshSaveSlots` at lines 132–147 uses a monotonically increasing token to prevent older responses replacing newer choices. The retained disabled button, twelve old options and already-created final save fit a pending latest list scan. This source evidence does not yet supply a measured scan duration; the root coordinator is measuring the existing disposable folder separately. No obvious lost-save or token sequencing bug was identified.

The smallest harness correction is to relocate only explicitly enumerated audit captures out of `run/saves` immediately after their successful native API save, before the unchanged archive worker inspection. Keep their exact bytes in `run/audit-captures`, with checked paths and exclusive targets. The genuine inputs and real named midpoint/final slots must remain visible; their original Save/Load/Continue assertions and 30-second waits stay unchanged. This reduces test-created save-list pollution without changing game state or discarding evidence. The full-scan cost remains a real limitation for users who keep many large saves; it is not removed by this harness correction.

The coordinator's subsequent retained diagnostic (`evidence/S08-save-menu-list-diagnostic-1/result.json`) measured one GET at 25.1878707 seconds and returned all 21 readable slots, including the final named save dated 29 November 1995. All file hashes were unchanged. This confirms the save exists and the metadata scan is expensive; it does not measure the complete pending refresh queue. A separately prepared harness-only capture isolation correction and its focused tests are described in `supplier-audit-capture-isolation-review.md`.

No repository files, game runtime, browser runs, tests or builds were changed or executed during the read-only review itself. The publication drafts retain S08 in progress and full supplier-browser acceptance pending.
