# S05 — Unified save, startup and command qualification

**Status: Complete. Runtime candidate: `db9d17c8d726aa102aa143ceb3599009c558ffee`.** This report separates executed predecessor evidence from final-candidate results. G1 is decided explicitly below; this is not full-campaign certification.

## Behavior delivered

Fresh campaigns explicitly initialize connected economy, historical party identities, company operations and operational warfare. Opening the physical market preserves existing stock cover; enrollment creates no supplier stock, paid work or simulated day. Loading preserves the archive's capabilities and requires explicit adoption of newly optional systems.

The save classifier preserves active supplier books in `companies`, maps the recognized master roster into `sector_contractors`, and declares equipment/party/economy/company/supplier/warfare versions. Company names and numeric IDs never imply shared ownership. Loading performs no settlement, purchase, delivery, succession or RNG draw.

Command receipts identify one action within a live session. Identical retries reconcile its committed effect; changed payloads and stale sessions refuse. Reloaded uncertainty remains visible for review. Saving stages complete data before replacement and retains a valid backup.

## Source and execution identity

The integration retains active `5f7f355502f17bd6bd8f0383a2d14f0024fa7884` and selectively incorporates master `485c223f60d5ff6e46f6ae17164bf1ee3a8764d9`. S03 predecessor is `8b1ff717ae5c21d1b96d183fbd987164ed10b03b`; S04 candidate is `ff446b60d7b28c756cca8a9fce3a0e0947fd4e5f`.

| Evidence lane | Source and result |
| --- | --- |
| Windows full workspace | Clean `8c9159786d3736a9953706262a52c50898225f0e`: **1,475 native passed, 0 failed, 75 ignored**; `S05-native-candidate-2.{json,log}`. |
| WSL GNU/Linux full workspace | Same clean `8c91597`: **1,475 native passed, 0 failed, 75 ignored**; `S05-linux-candidate-2/{runner-result,summary}.json`, `native.log`. |
| Windows and Linux Node | Same `8c91597`: **1,459 passed, 0 failed, 1 skipped** on each platform; corresponding candidate-2 UI/Node logs. |
| Final Windows full native | Clean `db9d17c8…`: **1,480 passed, 0 failed, 75 ignored**; [native record](evidence/S05-final-native-db9d17c.json), [full log](evidence/S05-final-native-db9d17c.log), exit 0. Web test binary SHA-256 `f0f92df8fe0f03c1325c6ba825e43514b9954ef11331ba6849677a5cefc02a96`. |
| Final Windows Node | Clean `db9d17c8…`: **1,472 passed, 0 failed, 1 skipped**; `S05-final-node-db9d17c.{json,log}`, exit0. |
| Final Linux full native + Node | Clean `db9d17c8…`: **1,480 native passed, 0 failed, 75 ignored; 1,472 Node passed, 0 failed, 1 skipped**; [Linux summary](evidence/S05-linux-runtime-final/summary.json), [runner](evidence/S05-linux-runtime-final/runner-result.json). Binary unchanged; fixtures unchanged. Local WSL GNU/Linux only. |
| Final external archive checks | All three separately invoked checks passed on Windows and Linux: genuine master archive, 28 original active lifecycle exports, and party wrappers for equipment versions 2–5. [Windows record](evidence/S05-external-runtime-final/result.json), [Linux summary](evidence/S05-linux-runtime-final/summary.json). |
| Final binary/browser | **PASS** on the db9 release binary SHA-256 `b641b6e96baa98c095fcdb239ff868ecbc5bbef2246a722d5387f0a9ffd324f3`; [browser result](evidence/S05-browser-db9d17c/result.json) and 11 PNGs record desktop 1440×1000/mobile 390×844, 20 served/checkout/committed asset identities, natural city close and Advisors/Tutorial/F1, government review/cancel, $0.2 bn Alaska construction preview, 30 company rows/filtering, persisted lost-response recovery, named-load Cancel/confirm, delayed list selection and new session/history. The government desktop and construction mobile screenshots were visually inspected for legibility. |
| Final all-capability performance | **PASS for both predefined confirmations**, each six copied workloads × 31 measured days with connected economy, company operations and warfare adopted. Simulation p95 ≤300 ms, whole-turn p95 ≤400 ms, whole maximum ≤750 ms and sampled private memory ≤1 GiB remain unchanged. Busy-year-30 p95 was **291.268/397.802 ms** (simulation/whole) in [confirmation 1](evidence/S05-lifetime-confirmation-1/profile.json) and **261.161/359.921 ms** in [confirmation 2](evidence/S05-lifetime-confirmation-2/profile.json). Sampled private maxima were 445,026,304 and 423,804,928 bytes. The [fixed protocol](evidence/S05-performance-confirmation-protocol.json) required both runs to pass, with no further retries. The earlier optimized run missed 300/400 ms at **303.450/413.267 ms**, and the pre-optimization run had larger failures; both remain recorded. Runner `passed` validates profile execution/structure; the stricter separate qualification also checks every numerical bar. The near-limit first confirmation leaves little headroom: timing variation is observed without an inferred cause, and S22 owns broader performance qualification. |

The source-bound logs, browser result/screenshots and provenance are packaged in [evidence](evidence/), indexed by the [SHA-256 manifest](manifest.json). Candidate db9 changes military route planning as well as the save-list UI. Its lazy movement graph is shared only against an immutable world, is rebuilt after capacity/access changes, and avoids route queries for zero available force. Exact path/ETA/purity tests accompany the change; the final suites cover this runtime. Performance bars remain unchanged. The lifetime runner validates enrollment before timing and actual campaign initialization after measured settlement. WSL native/filesystem results do not establish Linux browser or hosted GitHub Actions execution.

The real browser ran **Chrome 152.0.7977.83** through Playwright's supported installed `chrome` channel; [runtime/toolchain identity](evidence/S05-windows-browser-runtime.json) includes the wrapper hash. The downloaded headless-shell executable was absent even after a completed reinstall; no cause is inferred. The wrapper changes only launch options and leaves the committed assertions intact.

The 75 ignored tests are not passes; original archive checks require separate invocation. The one Node skip is the optional archived advisor renewal reading whose artifact is absent.

## S1–S8 acceptance matrix

The contract is [S01 runtime and validation](../S01/RUNTIME_AND_VALIDATION.md), S1–S8. Names below are exact test/function anchors. Web modules are under `spheres-web/src`; native lifecycle files are under `spheres-sim/tests`. The detailed evidence mapping records exact passing Windows/Linux log lines.

| Cell | Evidence and boundary |
| --- | --- |
| **S1 — dialects** | `storage::tests::envelope_load_preserves_sim_timeline_and_archive_legacy_still_loads`; `s05_save_matrix_tests::standalone_party_v1_with_no_equipment_and_native_equipment_v1_uses_both_loaders`. Combined company/integrated native and browser forms pass predecessor defaults. Original active 28 and party versions 2–5 pass separately on both OSes. Final master/active/party result: all three passed on both platforms, on the exact runtime candidate. |
| **S2 — refusal** | `s05_save_matrix_tests::malformed_archives_and_mismatched_direct_party_books_refuse_clearly`; `s04_integration::integrated_capabilities_refuse_downgrades_and_nested_unknown_property`; `s05_master_fiscal::retained_master_receipt_does_not_relax_current_or_corrupt_receipts`. Wrong versions/book pairs, missing fields, malformed history and corrupt receipts refuse; current state is replaced only after successful decoding. |
| **S3 — adoption** | `s05_fresh_startup_tests::fresh_start_adopts_all_capabilities_without_free_work_or_supplier_stock`, `fresh_startup_repeat_and_save_resume_preserve_exact_owned_state`, `legacy_load_keeps_company_and_operational_upgrades_opt_in`; six fresh-start tests pass. Midmonth replay and Japanese empty-component migration preserve dates/property and reject populated or mixed replacements. |
| **S4 — transactions** | `company_ammunition::s05_paid_company_air_stores_survive_pending_save_then_feed_operational_strike`; `company_refits::s05_partial_refit_and_paid_building_continue_once_across_all_capability_adoption`; `s05_save_matrix_tests::pending_peace_and_real_movement_survive_archive_then_consent_settles_once`. Actual paid work, reservations, travel, consent and losses survive and settle once. Fixtures disclose controlled prerequisites; this is not every mission/country combination. |
| **S5 — uncertainty** | `s05_campaign_api_tests::integrated_paid_orders_reconcile_exactly_once_and_changed_receipts_refuse`, `integrated_paid_day_retry_archive_reload_and_next_day_keep_one_timeline`; Node transport/session tests cover double clicks and persisted intent. Final `ci-browser.cjs` completes lost committed response → reload/Continue → exact visible retry → named load/new session, retaining the same two requests. |
| **S6 — storage** | `storage::tests::integrated_paid_work_survives_interrupted_write_and_valid_backup_recovery`, `failed_write_keeps_previous_valid_campaign_and_archive`, `slots_cannot_escape_root_and_three_autosaves_rotate` passed on both platforms. Staged-write failure, damaged-current/valid-backup recovery, history/log and once-only continuation are covered. Master archive is an explicit 18.8 MB representative; no arbitrary-size, real disk-full or process-kill claim. |
| **S7 — review isolation** | `government_view::tests::government_preview_requires_own_exact_current_action`; `companies_view::tests::directory_and_adoption_preview_are_pure_and_player_scoped`; `tests::construction_impact_preview_is_read_only_scoped_and_validates_the_reviewed_project`. Native/Node guards cover state/player/target changes and guidance without orders. Final live helper compared whole archives around government review/cancel, construction preview, company filtering and tutorial navigation. |
| **S8 — actual browser** | `ci-integrated.cjs::{verifyBuild,capabilities,panels}` and `ci-browser.cjs`: exact build/session/routes, 20 embedded assets, startup/save/load/research and government/construction/company panels at desktop/narrow widths. Served bytes equal checkout bytes; only checkout-to-commit comparison normalizes CRLF. Final browser status: passed on db9 at desktop 1440×1000 and mobile 390×844. |

## Original inputs and migrations

The original `fixtures/s05-original-active/active-fixture-manifest.json`, included in the [qualification fixture package](evidence/S05-fixture-archive/qualification-fixtures.zip), identifies **28 original lifecycle exports**: five tank, five mixed ground/air, four ammunition and seven refit stages each for ground/air. Their commands/ticks produced the paid states; opening prerequisites are disclosed. The importer checks native/browser roundtrips and deterministic continuation both without adoption and after explicit adoption. Separate party wrappers use real versions 2–5 supplier files. Exporter provenance includes exact logged paths, unchanged pinned source, fingerprints/timestamps and current before/after binary hashes; those hashes are not retroactive S01 cryptographic pins.

The genuine master generator has recorded executable SHA-256 `2bedd29560bed02e157a6953d64c45faf91ee21aeac85083aed78a3e74f4f622`. Its 1 February 1990 archive SHA-256 is `5fba07caf82335609aab312f14a37f775dcac84e4821bddc36ab63a5bf795463` (`fixtures/s05-pinned-master/generation.json`). It includes 763 contractors, 244 assignments and actual war/population/fiscal books; absent active supplier/party assets are not invented.

Recognized master stale ordinary receipts are retained as exact dated provenance, without backfilling observations or changing original pending data, nominal balances or dates. Other stale/corrupt receipts still refuse. The documented Japanese empty-component upgrade likewise accepts only its exact empty legacy shape.

The [deployment comparison](evidence/S05-deployment-outcome-optimized/result.json) passes all five exact canonical archive comparisons against the pre-optimization baseline: days 0, 1, 15 and 31, plus the day-15 reload. The [preservation audit](evidence/S05-preservation-runtime-final.json) records eight protected saves, 28 original active exports, the genuine master archive, four original binaries and both pinned worktrees unchanged. Full canonical comparison archives (approximately 200 MB) remain in the isolated local workspace; packaged records retain their hashes, the reproducible runner and compressed original inputs. [Fixture extraction and runner notes](evidence/S05-fixture-archive/README.md) distinguish these inputs from generated outputs.

## Failures retained and corrected

| Observed issue | Correction checked on the final candidate |
| --- | --- |
| Empty legacy military `{}` rejected; duplicate-date party test archive invalid. | Normalize only exact empty/null military books; remove the test's redundant snapshot and retain archive comparisons. |
| Genuine master stale receipt refused. | Preserve its exact historical receipt as above; current/corrupt receipt guards remain strict. |
| Master comparator promoted native `f32` into different JSON decimals. | Compare exact serialized wire values and native `f32` bits; no numeric tolerance. Candidate-2 failure remains recorded. |
| Delayed boot replaced the user's Saves view with Home. | Preserve the selected menu view on success/error; delayed-native-response browser regression. |
| Guidance intercepted Construction, then overlapped Close city after being raised; near-zoom water/cities failed review. | Campaign uses standard Advisors/Tutorial/F1 and suppresses the redundant launcher; standalone default remains. The `93a49f5` CI completed natural city-close checks before its later Saves failure. Manual water/picking corrections and successor reload are source-bound below. |
| CI used stale Advisor/research selectors and omitted guarded Companies session IDs. | Correct the harness to current visible controls/API contract; retain natural clicks and original assertions. |
| The `93a49f5` named-load Cancel/confirm sent an older selected slot. | db9 preserves the latest selection when delayed `/api/saves` data arrives and ignores older responses. Five tests exercise real refresh/load functions; final CI holds the untouched native response through confirmation and observes the actual select rebuild. Expected `ci-smoke` and Cancel remain unchanged. |
| Military route planning rebuilt identical freight graphs unnecessarily. | db9 uses a lazy immutable movement context and per-nation hubs; route order/ETA and changed-access/capacity tests retain semantics. Final timings are recorded above against the original bars; no target was relaxed. |

The first optimized db9 lifetime run retained a performance failure: busy year 30 measured simulation p95 **303.4502 ms** against 300 ms and whole-turn p95 **413.2666 ms** against 400 ms (maximum 545 ms passed the 750 ms bar). A structural runner success is not a throughput pass. The remaining five scenario/checkpoint rows passed; this run cannot award G1. Its record is [the first optimized lifetime attempt](evidence/S05-lifetime-runtime-final/runner-result.json). A [written confirmation protocol](evidence/S05-performance-confirmation-protocol.json), recorded before the followups, permits exactly two further unchanged six-case runs and requires both to meet every original bar. Final outcomes are reported above; the failed first run remains visible.

Initial logs are retained. The bounded history fix also preserves achieved campaign goals while retaining only the latest 24 abandoned selections; its controlled test does not represent completed campaigns.

## Decisions and stop boundary

**S05 completion: complete for the recorded save, startup, command and browser qualification scope. G1 decision: earned locally for the pinned unified playset; full 1990–2035 campaign certification remains unearned, recorded separately.** Final evidence/limitations: S06 remains planned. All-137 starts, all-23 successors, eight-country seeded 1990–2035 runs, independent human playtests, complete historical portraits/art and packaged-release certification retain later owners. Namibia has no birth site; Lake Geneva is not in the authored lake paths. Procedural city detail and ~1,855 m elevation spacing remain explicit limits; supported flight missions do not imply every aircraft/mission family.. Git, clean-state, artifact and remote disposition: Runtime `db9d17c8d726aa102aa143ceb3599009c558ffee` was clean before and after the final suites. The closeout commit adds only documentation/evidence. Original active and master sources, eight protected saves and original qualification inputs remain unchanged. GitHub publication is separate: recorded preflight push attempts lacked authentication; the final push outcome is reported in the task. No hosted GitHub Actions result is claimed.. The earlier server-execution restriction was resolved through authorization and is not a current blocker.

Actual map observations are recorded in [the corrected map review](evidence/S05-live-map-final/visual-observations.json) at `2618979` (1280×720) and [the successor reload review](evidence/S05-live-map-93a49f5/visual-observations.json) at `93a49f5` (actual 2555×1272). The latter confirms native reload of `successor-reviewed`, natural Close city and 1500× Ljubljana ground picking into Osrednjeslovenska/Slovenia while paused on 1 February 2000. These are unchanged-map-asset observations, not manual db9 execution. Namibia has no birth site; Lake Geneva has no authored lake coverage. Neither is claimed as passed. Full successor activation belongs to S10/S24.

No S06 work begins. All-137 starts, all-23 successor coverage, eight-country seeded 1990–2035 campaigns, independent human playtests, complete art/content and packaged-release certification retain their pathway owners. S05 makes no full-2035 certification claim.

## Preservation and routing equivalence

The [final preservation audit](evidence/S05-preservation-runtime-final.json) confirms all eight protected saves, all 28 original active fixture files, the genuine master archive, four producer binaries and two original source worktrees remain unchanged. Original active HEAD is `038fe0b1a3edb1772586ec0b5a7451629b7297c4`; its runtime/data baseline remains `5f7f355502f17bd6bd8f0383a2d14f0024fa7884`. The original master checkout remains exact `485c223f60d5ff6e46f6ae17164bf1ee3a8764d9`.

The [pre/post routing comparison](evidence/S05-deployment-outcome-optimized/result.json) loads the same genuine master archive on pre-optimization93 and final db9, advances 31 normal days, and reloads its native save at day15. All five checkpoint archives match exactly, including world, history, dispatch log and RNG. Only top-level wall-clock `saved_unix` is omitted; numeric tokens retain exact u64/floating-point values. The final canonical archive SHA-256 is `d99e40ad18bd39b9e0bb168f7a000ecaf89285d4aadffcd72e70594feb95b65a`. Full output archives stay local; their hashes, requests and runner are packaged with [original input extraction instructions](evidence/S05-fixture-archive/README.md). This is a real technical war workload, not a claim of a certified USA campaign.
