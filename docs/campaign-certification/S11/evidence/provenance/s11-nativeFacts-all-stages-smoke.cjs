const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm'),assert=require('node:assert/strict'),cp=require('node:child_process');
const base=__dirname,source=fs.readFileSync(path.join(base,'s11-nativeFacts-patch.cjs'),'utf8'),fixture=path.join(base,'evidence/S11-final-native-fixture'),manifest=JSON.parse(fs.readFileSync(path.join(fixture,'manifest.json'))),output=path.join(base,'evidence/S11-browser-reader-debug'),run=path.join(output,fs.readdirSync(output).find(x=>x.startsWith('france-'))),e=JSON.parse(fs.readFileSync(path.join(run,'result.json')));
const context=vm.createContext({cp,assert,process,requiredOutcomes:manifest.required_outcomes,copy:x=>JSON.parse(JSON.stringify(x)),near:(a,b,label)=>assert(Math.abs(a-b)<1e-9,label)});
for(const [start,end] of [['function nativeFacts(','\nfunction inspect('],['function verifyGround(','\nasync function groundReading(']]){const first=source.indexOf(start),last=source.indexOf(end,first);vm.runInContext(source.slice(first,last),context);}
// Pass values through the same JavaScript realm as the extracted shipped functions.
for(const stage of e.stages){const native=JSON.parse(fs.readFileSync(path.join(run,stage.id+'-state.json'))),facts=context.nativeFacts(stage.archive.path);context.nativeJson=JSON.stringify(native);context.factsJson=JSON.stringify(facts);vm.runInContext('verifyGround(JSON.parse(nativeJson),{ground:JSON.parse(factsJson)})',context);stage.as_of_day=facts.as_of_day;}
const outcomes=context.verifyOutcomes(e,manifest);fs.writeFileSync(path.join(base,'s11-nativeFacts-all-stages-smoke-result.json'),JSON.stringify({passed:true,diagnostic_only:true,stages:e.stages.length,days:outcomes.days_advanced,outcomes},null,2));console.log(JSON.stringify({passed:true,stages:e.stages.length,days:outcomes.days_advanced}));
