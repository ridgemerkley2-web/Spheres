# S10.d — Clear diplomatic replies

**S10.d complete; S10, C01 and G2 remain open.** Development stopped after this
bounded increment. S11 has not started.

[Play the updated review build](http://127.0.0.1:7850/).

The Decisions room puts pending requests ahead of optional campaign aims and
orders them by their exact native deadline, including overdue requests whose
remaining days have reached zero. **Review acceptance** and **Review decline**
open a preview; only confirmation submits an order. The review retains the
sender, request type and deadline, even when the native command refuses it.

Deadlines now say **Reply before**: the reply window closes when that date begins.
Guarantee warnings describe conditional consequences instead of promising fixed
losses when native caps or changed circumstances alter the result. Exact native
before/after values remain authoritative. Completed replies receive a visible,
focused notice. Effect values stack at 320px, and the Close button stays on one line.

The avatar API now carries the existing derivative artwork license separately
from the reference-image license. Government attribution shows both safely.
Tupou IV remains bound to the same reviewed person and exclusive 1990 art window.

## Bounded Japan research

[Japan's packet](../../C01/research/japan.json) records 16 election-list
organizations from July 2025 and seven separately classified House parliamentary
groups from February 2026. Four party offices contain five dated holder
observations. Term endpoints and game mappings remain unknown.

Seven primary sources support the packet. Two retained factual extracts have
checksums; their response hashes identify fetched pages but do not retain raw
HTML or allow those absent response bytes to be independently checked offline.
Historical observations respect the fixed **7 September 2026** cutoff; access
on 13 September is recorded separately. These discoveries do not establish an
exhaustive country census or complete leadership histories.

The shared intake now has four country packets, 660 organization observations,
18 institution observations, 38 sources, 1,344 claims and 70 research batches.
Zero exhaustive country censuses are closed.

## Verification and exact scope

- Final Windows native web suite: **371 passed, 0 failed, 12 ignored**.
- Full Windows interface suite: **1,537 passed, 0 failed, 1 skipped**.
  The 87 focused interface tests also passed after the final CSS change.
- Windows and Linux agency/succession integration: **12 passed on each**.
  Linux native web: **371 passed, 0 failed, 12 ignored**.
- Content: **40 tests** and five reproduction/allowlist checks passed in nine
  commands, including the new Japan boundary tests.
- The final authored browser fixture tests three legally queued native requests:
  a trade treaty, a defense pact and a guaranteed call to arms. All six
  accept/decline previews match native execution; all six cancellations preserve
  the complete world. Exactly one confirmed trade reply matches the independently
  executed native result, including relation caps, treaty depth and the saved
  ledger. Named Save, cancelled Load, Load and Continue preserve it.
- A separate ordinary France journey passed real second-tab staleness,
  refusal without mutation, fresh government confirmation and Save/Load/Continue.
- Both final browser journeys passed desktop, 390px and 320px checks. The
  authored case also verifies Close on one line and settled result focus without
  test-side scroll correction. Sixteen native archive inspections and eleven
  complete comparisons ignore no world fields.

The pending-request fixture deliberately authors its starting diplomatic
conditions, political capital and relations. Its manifest lists every setup
change. It is not historical opening diplomacy or evidence of an ordinary war
arising during play. Both browser journeys advance **zero campaign days**.
No long-campaign, performance, Russia-activation or new full-save-matrix
qualification is claimed. The earlier eight-country startup evidence remains
in S10.c; it was not rerun here.

## Source and preservation

Final runtime: `6ab4d2fccd1128cff14ecb7470aac82b9105e291`.
Executable SHA-256:
`45b113ea8885c155136d4e7e877e3208e72d452d9a9b2b703d2294e599342783`.

Full Node, Linux and content checks used
`2b42cbeb44774ecdd31ca049cff3e89647269c73`. The only final changes were one
Close-button CSS rule and stronger browser assertions; all Rust, game data and
application JavaScript are identical between these revisions. Windows native
and both browser journeys were then rerun on the final runtime.

Visual review found the original 320px Close-label wrap after automated checks
passed. That screenshot is retained under development evidence; the final
browser tests explicitly cover its correction. A clean-source preflight also
stopped before execution while the browser comparator was being finalized.

The [manifest](manifest.json) binds the [complete selected evidence](evidence/inventory.json):
125 logical files, 76,707,801 stored bytes. Large archives are compressed and
reconstructed byte for byte. Compiled binaries are identified by hash rather than
stored again. All eight protected originals, two source worktrees and previous
review servers were preserved. The review server uses its own verified save copy.
