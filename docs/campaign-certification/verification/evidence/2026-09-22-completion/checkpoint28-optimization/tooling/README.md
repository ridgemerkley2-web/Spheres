# Checkpoint28 provenance verification

The parent restored the rejected candidate27 source, then added an empty-stall-mask early return in dyads.rs. Seven leadership-production, census and research commands passed on that frozen source. Production self-tests: 8; census unit tests: 7.

Every generated file remains byte-identical to checkpoint26. No historical records, art, or provenance metadata required modification. All protected source bytes and modification times, including government.rs and dyads.rs, remained unchanged throughout. The wrapper refuses a noncanonical government source rather than rewriting it.

Full JavaScript tests were not repeated because no JavaScript or generated input changed; checkpoint26 already passed 1,701 tests. This receipt is not a native result, political acceptance result, or expanded historical-coverage claim. Hosted checkpoint26 is monitored separately at exact9f7faed5.
