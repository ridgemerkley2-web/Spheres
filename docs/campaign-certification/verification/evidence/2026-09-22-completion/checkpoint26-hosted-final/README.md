# Checkpoint26 hosted CI: complete, A1 still fails

[Run 35794328514](https://github.com/ridgemerkley2-web/Spheres/actions/runs/35794328514) tested exact pushed commit `9f7faed5f541e14545e01caf1616ba08c6ad8ff9`. All ten jobs completed: six passed and four failed.

Both Windows and Ubuntu passed 1,886 workspace tests across 66 targets (89 ignored; one resource timing test filtered), then passed that timing test in isolation, built the server, and passed the complete CI browser journey. Both JavaScript/tools and town-render jobs also passed.

Each political job executed the ten original outcome gates plus attribution controls: ten tests passed and only A1 failed. Its observed median was seven elected-government coups; median top-three country share was 0.59 against the unchanged requirement below 0.50. A2-A10 and the classifier passed. Both aggregate jobs correctly failed solely on political-calibration. This is not an all-green campaign certification.

The six logs preserve exact connector-decoded UTF-8 text and platform line endings. Artifact metadata identifies the available browser evidence; no ZIP artifacts were downloaded. The SHA-256 manifest is checked against raw bytes and Git-filtered blob equality. The ignored diagnostic/export/archive tests are not presented as hosted passes. Separate local archive receipts cover selected opt-in checks.

This result applies only to checkpoint26. Subsequent rejected model experiments and dyads optimizations have separate evidence.
