# Opening mandate and A1 audit evidence

Read-only review, 22 September 2026. This packet changes no simulation source, historical roster, vote share, threshold or acceptance result. A1 remains failed on the pinned iteration12 development sample; later confidence candidates are recorded elsewhere.

- `iteration-12-a1-independent-audit.md`: statistic, actual concentration, current mechanism scan and predeclared confidence-grid implications.
- `iteration-12-a1-statistical-audit.json`: arithmetic on the existing200-seed log, with log SHA256 and every parsed row. This is development data, not a held-out test. The original criterion is unchanged.
- `iteration-12-development-n200-provenance.json`: exact binary hash and original measurement command. Its manifest/log references point to the preserved local political-calibration evidence, not files duplicated in this packet.
- `opening-mandate-six-case-audit.md` and `.json`: six requested country cases, code fields, dates, incumbent ties, restrictions and bounded design implications.
- `primary-source-receipts.json`: URLs, locators, access limitations and exact response hashes where an independent HTTP fetch succeeded. Web-tool readings are recorded separately from direct-fetch failures; no inaccessible document is represented as fully read.
- `artifact-hashes.json`: byte hashes for this packet, excluding the hash manifest itself.

The numerical problem is real: the latest reviewed sample has1,574electoral coups; its pooled top3 share is54.19%, with six countries accounting for97.08%. The per-seed metric is also strongly discrete: fewer than7positive events cannot pass, and7or8events require every event in a different country. These observations diagnose the existing failure; neither is a substitute acceptance rule.

A prior public mandate and a completed in-game ballot are different facts. Only Suriname and Guatemala among these six have documented matching pre-1990 competitive-ballot incumbents. Suriname retained substantial military powers; its exact85.5% was not independently reverified (official result PDF inaccessible, CIA's figure coarse). Guatemala's actual DCG majority does not substantiate the game's synthesized DCG+UCN coalition as an inherited administration, and the PGT was legally excluded. Guyana's declared result carries fraud allegations. Comoros, São Tomé and Myanmar use future results relative to the campaign opening. Do not blanket-set `elected` or `unrestricted_mandate` from these findings.

Source snapshots and audit prose may reference the then-current shared source, while measured outcomes remain tied to the iteration12 compiled manifest. No later source edits are silently attributed to that binary. The underlying source comments' older measurements and candidate11 lifecycle observations remain historical context, explicitly identified as such.
