# Pinned-master load repair

Checkpoint25 `7b8a3dbb90f191ee5520705a8233409274d9d752` passed its ordinary workspace suite, then failed one of four genuine archived-save checks. The original `actual_pinned_master_archive_preserves_property_history_and_next_day` test detected that `governments.states[0].pillars` grew from zero to one while loading. Its fixture and exact property assertions remain unchanged. All 32 original fixture/provenance files were hash-identical after all four checks; source inputs were also unchanged during execution.

The cause is `storage::decode -> loaded_play_game -> play_rules -> government::ensure_all -> ensure`: the new sourced-institution retrofit appended Army loyalty0.65 to an already recorded saved government. This was a real load-time mutation, not a numeric formatting mismatch or a bad historical quota. A prior unit test explicitly demanded this old-save upgrade; that contract conflicts with the genuine archive-preservation requirement. Its old body is retained beside this record and superseded by preservation checks, without altering the archived-save test.

The repair preserves existing `GovState.pillars`, including an empty list or a nonempty list without Army. A wholly absent government remains eligible for the established lazy constructor; real future model transitions remain distinct from merely reading an archive. Tests preserve live loyalty, all existing government fields, RNG, off-path saves and repeat load/ensure.

Fresh browser games previously constructed a default/lens-off world and enabled the lens later. That bypassed some sourced opening state. Actual boot and new-game now use private `Game::new_fresh`, which passes the lens to the authoritative `world_1990` constructor before any government is seated. The default/off constructor remains for legacy tests. No duplicate public initialization API or January1 heuristic was added. Fresh-only fixture generators use the same fresh constructor. A full GovState comparison covers every opening government, including Afghanistan's established movement and Suriname's attested prior mandate. Repeated browser capability adoption preserves live loyalty and mutable authority. A saved January1 government still follows the load path and retains unknown source fields and its recorded pillars; resumed next-day state is compared exactly.

This intentionally corrects both load preservation and fresh-browser initialization; it is not a claim of no production changes. New native tests and the unchanged real archive test await the parent-controlled build. No calibration coefficient, gate, source dataset, archive or historical baseline was changed.
