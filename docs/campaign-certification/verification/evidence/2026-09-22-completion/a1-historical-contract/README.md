# A1 historical contract review

Read-only review completed 2026-09-22. `audit.md` separates historical taxonomy from the unchanged A1 acceptance rule. It does not certify the current source, replace a threshold, or convert a failed gate to a pass. Simulation figures explicitly identify iteration12; later candidates are outside this receipt.

The author-provided `pt_20260829.csv` is preserved byte for byte, including its original CRCRLF record separators. A scoped Git attribute marks it as binary text data (`-text`) so checkout and clean conversion cannot alter the recorded SHA256. It remains a CSV readable by ordinary CSV libraries. `dataset-window-summary.json` contains the exact inclusive1990–2010 filter, all rows, counts and limitations. The full historical population includes unelected governments and countries absent from the game: it is not an A1-matched sample.

`research-receipts.json` distinguishes documents actually read from inaccessible documents and third-party republications of primary material. The linked current codebook was HTTP403; its contents were not assumed. The original author-hosted2011 paper supplies the definition instead. No exact numerical replacement target was proposed.

`artifact-hashes.json` records the six accompanying files. All text files are LF; the CSV is intentionally original bytes. Hashes were checked against both working bytes and Git-clean blob bytes before handoff. There were no simulation edits or Cargo runs for this audit.
