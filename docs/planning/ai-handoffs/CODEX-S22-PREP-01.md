# CODEX-S22-PREP-01 — art accounting and shadow compiler repair

Owner: Codex. State: implementation prepared; exact-source validation pending.
Branch: `codex/campaign-certification`; base: `7a13135e2a26a007eaf161b799ffc9e2f582a43d`.
Parent: S22 preparation. S22 remains planned and depends on S20; this packet
does not claim the performance session or its campaign qualification is complete.

Claude S19 remains claimed on its existing branch. This independent packet
touches art measurement tools, generated art records, one shadow shader identifier
in `spheres-web/ui/equipment-model.js`, and its own evidence/documentation.
It does not edit the shared navigation shell, guidance modules or simulation.

The old art audit rejected the current `materialClasses` array. Repair its
explicit layout accounting, separate CPU backing buffers from base upload
payloads, and measure all twelve ground/air baseline detail levels. Preserve
the original budget limits and failing budget gate. Record freshness is a
separate check and must not be reported as a budget pass.

An isolated Chrome probe reproduced a shader compiler error: `packed` is a
reserved word. The renderer silently fell back to projected shadows. Rename
that local variable and require a real offscreen shadow pass, successful shader
compilation, unchanged buffer storage during orbit, replacement through LODs,
context recovery, disposal and a fresh visit in the browser regression.

Validation must pin a clean commit, retain before/after browser observations,
check generated records and focused renderer/accounting tests on Windows and
Linux, and build a separate review executable. Do not infer FPS, total VRAM,
city-cache qualification or a full campaign result from this isolated bench.

Next: integrate Claude's S19 submission when ready, complete S20, then use these
tools for S22's actual early/mid/late campaign performance qualification.
